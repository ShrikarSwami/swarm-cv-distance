"""Swarm Reconstruction Tool — distributable package.

Pluggable reconstruction backends, scene browser, 3D visualisation, and
adjacency-matrix export for GA/PSO consumption.

Usage:
    python -m tool --scene 0 --mode all --d-max 15.0
    python -m tool --list
    python -m tool --scene 5 --mode random-n --n 8 --d-max 15.0 --out /tmp/recon.png
"""

__version__ = "1.0.0"
